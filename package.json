{
  "name": "claude-review-guide",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx --yes concurrently -n fe,be -c cyan,magenta \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && .venv/bin/uvicorn app.main:app --reload --port 8000",
    "install:all": "cd frontend && npm install && cd ../backend && uv venv .venv && uv pip install -e .",
    "build": "cd frontend && npm run build"
  }
}
